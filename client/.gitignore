# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets
# NOTE: every VITE_-prefixed variable is inlined into the public bundle.
# Never put a secret in client/.env — it is served to every visitor.
.env
.env.*
!.env.example

# Credential files
*.pem
*.key
*credentials*.json
serviceAccount*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
